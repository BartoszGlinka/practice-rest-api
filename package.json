{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "4.17.1",
    "nodemon": "^2.0.4",
    "uuid": "^8.3.0",
    "yarn": "^1.22.5",
    "yyid": "^2.0.0"
  },
  "engines": {
    "node": "10.x"
  },
  "scripts": {
    "start": "nodemon server.js",
    "build": "cd client && yarn install && yarn build"
  }
}
